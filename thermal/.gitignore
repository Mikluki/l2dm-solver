# Mesh and artifact caches - never committed (ADR-0007, ADR-0008).
tests/_artifacts/
tests/_mesh_cache/

# Human-comprehension artifacts from scripts/inspect.py. Regeneratable.
artifacts/inspect/problem_*/*

# Parent l2dm-solver/.gitignore has `**test.py` which silently matches
# `conftest.py`. Re-include the pytest conftest explicitly.
!tests/conftest.py

# Python tooling
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environment
.venv/
