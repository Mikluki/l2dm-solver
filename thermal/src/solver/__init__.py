# ABOUTME: Package marker for src.solver. Stateless functions of (Problem,
# mesh_size) that build the mesh, assemble, apply BCs/pin, and produce a
# SolverResult. No persistent state, no plotting, no error norms.
